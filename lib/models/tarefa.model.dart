class Tarefa {
  // atributos da classe:
  String id;
  String texto;
  int quantidade;
  final bool finalizada;
  

  // construtor:
  Tarefa({this.id, this.texto, this.quantidade, this.finalizada = false});
}
